# Text Analyzer & Reverser

A lightweight C program that analyzes text input and provides statistical insights along with string reversal functionality.

## Features ✨
- ✅ Count alphabetic characters (`a-z`, `A-Z`)
- ✅ Count numeric digits (`0-9`)
- ✅ Count words (space-delimited tokens with edge-case handling)
- ✅ Reverse the entire input string
- ✅ Safe input handling with `fgets()` and newline sanitization

## Usage 🚀
```bash
gcc text_analyzer.c -o analyzer
./analyzer